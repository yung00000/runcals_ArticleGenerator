# Supabase Configuration
SUPABASE_URL=https://dyvcdtsxrwyoevgsiuwf.supabase.co
SUPABASE_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Database Connection - Option 1: Use DATABASE_URL (Recommended)
# Transaction Mode Pooler (port 6543) - Matches pgAdmin 4 connection settings
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres.dyvcdtsxrwyoevgsiuwf:your_password@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres

# Database Connection - Option 2: Use Individual Parameters (Alternative)
# These match pgAdmin 4 connection settings exactly
# Uncomment and fill these if you prefer individual parameters instead of DATABASE_URL
# user=postgres.dyvcdtsxrwyoevgsiuwf
# password=your_password
# host=aws-1-ap-southeast-1.pooler.supabase.com
# port=6543
# dbname=postgres

# Password Encoding Notes:
# - Special characters must be URL-encoded in DATABASE_URL
# - Examples: # becomes %23, $ becomes %24, @ becomes %40
# - If password is ""pass##123"", use ""pass%23%23123"" in DATABASE_URL
# - Individual parameters (user/password/host/port/dbname) handle encoding automatically

# Security Settings
# Generate a strong API key: python -c ""import secrets; print(secrets.token_urlsafe(32))""
API_KEY=your-secret-api-key-here-change-in-production
API_KEY_HEADER=X-API-Key
RATE_LIMIT_ENABLED=True
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# Application Settings
APP_NAME=runcals_ArticleGenerator
APP_VERSION=1.0.0
DEBUG=True
ENVIRONMENT=development

# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS Settings (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080
